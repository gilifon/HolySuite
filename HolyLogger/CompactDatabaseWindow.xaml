<Window x:Class="HolyLogger.CompactDatabaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compact the Log Database" Width="560" Height="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBg}"
        FontFamily="Segoe UI">
    <Grid Margin="22,18,22,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0"
                   Text="Compact the log database"
                   FontSize="18" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   Margin="0,0,0,14"/>

        <Border Grid.Row="1" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1"
                CornerRadius="4" Background="{DynamicResource PanelBg}" Padding="12,10,12,10">
            <TextBlock x:Name="SizeText" FontSize="16" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextBrush}"/>
        </Border>

        <TextBlock x:Name="ExplainText" Grid.Row="2"
                   Margin="0,14,0,0" FontSize="16" TextWrapping="Wrap"
                   Foreground="{DynamicResource TextBrush}"/>

        <ProgressBar x:Name="Spinner" Grid.Row="3"
                     IsIndeterminate="True" Height="5" Margin="0,10,0,0"
                     Foreground="{DynamicResource AccentBrush}" Background="Transparent"
                     Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CompactBtn" Content="Compact now" Width="150" Height="34" FontSize="16"
                    Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource SelectionText}"
                    BorderThickness="0" IsDefault="True" Click="CompactBtn_Click"/>
            <Button x:Name="CloseBtn" Content="Close" Width="110" Height="34" FontSize="16"
                    Margin="10,0,0,0" IsCancel="True" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
