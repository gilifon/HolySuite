<Window x:Class="HolyLogger.ImportIdentityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Imported log identity" Width="490" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" Background="{DynamicResource WindowBg}">
    <Grid Margin="22,18,22,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Header" Grid.Row="0" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"
                   TextWrapping="Wrap" Text="Confirm the identity of the imported log"/>
        <TextBlock Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap" FontSize="16" Foreground="#666666"
                   Text="The station callsign is read from the ADIF file and can't be edited. If the file has no operator, add it — an identity needs both. This is permanent for the log you're importing into. Cancel stops the import."/>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Station callsign" VerticalAlignment="Center" FontSize="16"/>
            <ComboBox x:Name="CB_Station" Grid.Row="0" Grid.Column="1" Margin="0,0,0,6" FontSize="16"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Operator" VerticalAlignment="Center" FontSize="16"/>
            <ComboBox x:Name="CB_Operator" Grid.Row="1" Grid.Column="1" FontSize="16" IsEditable="True"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="Btn_Cancel" Content="Cancel import" Width="130" Height="32" FontSize="16" Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="Btn_Ok" Content="Confirm and import" Width="170" Height="32" FontSize="16" FontWeight="Bold"
                    IsDefault="True" Click="Btn_Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
