<Window x:Class="HolyLogger.NewLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log name" Width="460" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False" Background="#EEF2F7">
    <Grid Margin="22,18,22,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Prompt" Grid.Row="0" FontSize="14" FontWeight="Bold" Foreground="#1565C0"
                   TextWrapping="Wrap" Text="Enter a name for the log:"/>
        <TextBox x:Name="TB_Name" Grid.Row="1" Margin="0,10,0,0" FontSize="15" Padding="5,4"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="Btn_Cancel" Content="Cancel" Width="100" Height="32" FontSize="14" Margin="0,0,8,0"
                    IsCancel="True"/>
            <Button x:Name="Btn_Ok" Content="OK" Width="100" Height="32" FontSize="14" FontWeight="Bold"
                    IsDefault="True" Click="Btn_Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
