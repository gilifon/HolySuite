<Window x:Class="HolyLogger.ProfileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profile name" SizeToContent="Height" Width="420"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{DynamicResource WindowBg}"
        Foreground="{DynamicResource TextBrush}">
    <StackPanel Margin="16">
        <TextBlock x:Name="TB_Message" FontSize="16" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <TextBox x:Name="TB_Name" FontSize="16" Padding="4,3" KeyDown="TB_Name_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="OK" IsDefault="True" Padding="26,5" FontSize="16" Margin="0,0,10,0"
                    Click="Ok_Click"/>
            <Button Content="Cancel" IsCancel="True" Padding="20,5" FontSize="16" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
